export const CHANGELIKE = (id) => {
    return {
        type: "CHANGELIKE",
        payload: id
    }
}

export const TOGGLEDOTS = (id) => {
    return {
        type: "TOGGLEDOTS",
        payload: id
    }
}